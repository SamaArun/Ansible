- name: nginx install and run
  host: frontend
  tasks:
  - name: install nginx server
    ansible.builtin.package:
      name: "{{ nginx }}"
      state: present

- name: start nginx
  ansible.builtin.service:
    name: nginx
    state: restarted
    enabled: yes
